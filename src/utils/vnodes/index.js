import {VNode} from 'vue'
/**
 * @description
 *递归 拉平vue节点数组，
 * @param {*} [vnodes=[]]
 */
function flattenVNondes(vnodes=[]){
    const result=[];

    //遍历层次数组
    function traverse(vnodes=[]){
        vnodes.forEach((vnode)=>{
            result.push()
        })
    }
}